% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsea.R
\name{perform_ora}
\alias{perform_ora}
\title{Perform Over-Representation Analysis (ORA) for Gene Clusters}
\usage{
perform_ora(
  clusters,
  gene_sets,
  universe = NULL,
  p_adjust_method = "BH",
  min_set_size = 5,
  max_set_size = 500,
  p_threshold = 0.05
)
}
\arguments{
\item{clusters}{Named vector of cluster assignments (gene names as names, cluster IDs as values)}

\item{gene_sets}{Named list of gene sets/pathways (each element is a vector of gene names)}

\item{universe}{Vector of background genes. If NULL, uses all genes from clusters and gene_sets}

\item{p_adjust_method}{Method to adjust p-values for multiple testing (default: "BH")}

\item{min_set_size}{Minimum number of genes required in a gene set to be tested}

\item{max_set_size}{Maximum number of genes allowed in a gene set to be tested}

\item{p_threshold}{P-value threshold for significance (used in summary functions)}
}
\value{
List of ORA results per cluster, each containing a data frame with enrichment statistics
}
\description{
Performs hypergeometric test for pathway enrichment in gene clusters.
This is the main enrichment analysis function for clustered gene expression data.
}
\examples{
# Basic ORA analysis
clusters <- c(1, 1, 2, 2, 3, 3, 1, 2)
names(clusters) <- c("GENE1", "GENE2", "GENE3", "GENE4", "GENE5", 
                     "GENE6", "GENE7", "GENE8")
gene_sets <- list(
  Pathway1 = c("GENE1", "GENE2", "GENE7", "GENE9", "GENE10"),
  Pathway2 = c("GENE3", "GENE4", "GENE8", "GENE11", "GENE12")
)
ora_results <- perform_ora(clusters, gene_sets)
}
