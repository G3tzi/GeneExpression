% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_heatmap}
\alias{plot_heatmap}
\title{Plot heatmap of expression data with optional clustering annotation and scaling}
\usage{
plot_heatmap(
  eset,
  clusters = NULL,
  main = NULL,
  show_row_names = FALSE,
  scale = c("none", "row", "column")
)
}
\arguments{
\item{eset}{ExpressionSet object}

\item{clusters}{Named vector of cluster assignments for genes (optional)}

\item{main}{The graph's title}

\item{show_row_names}{Logical, whether to display gene names (default FALSE)}

\item{scale}{Character, one of "none", "row", or "column" for scaling expression data (default "none")}
}
\value{
Heatmap object
}
\description{
Plot heatmap of expression data with optional clustering annotation and scaling
}
\examples{
expr_matrix <- matrix(rnorm(1000), nrow = 100, ncol = 10)
colnames(expr_matrix) <- paste0("Sample", 1:10)
rownames(expr_matrix) <- paste0("Gene", 1:100)
gene_clusters <- kmeans(expr_matrix, centers = 3)$cluster
eset <- Biobase::ExpressionSet(
  assayData = expr_matrix
)
plot_heatmap(eset, clusters = gene_clusters, scale = "row")
}
