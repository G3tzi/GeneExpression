% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{cluster_expression}
\alias{cluster_expression}
\title{Unified clustering function with multiple methods}
\usage{
cluster_expression(
  eset,
  method = c("hierarchical", "kmeans", "dbscan"),
  k = 3,
  target = "gene",
  ...
)
}
\arguments{
\item{eset}{ExpressionSet}

\item{method}{One of "hierarchical", "kmeans", "dbscan"}

\item{k}{Number of clusters (used for hierarchical and kmeans)}

\item{target}{Cluster by "gene" (default) or "sample"}

\item{...}{Additional parameters passed to method}
}
\value{
A list with model object and cluster assignments
}
\description{
Unified clustering function with multiple methods
}
\details{
If `target = "sample"`, samples (columns) are clustered by transposing the expression matrix. 
If `target = "gene"`, gene (rows) are clustered directly.
The `cluster` vector in the returned object is named by gene or sample names accordingly.
}
\examples{
# Hierarchical clustering with automatic cluster assignment
data(eset)
hc_result <- cluster_expression(eset, method = "hierarchical", k = 4)
table(hc_result$clusters)

# K-means clustering
km_result <- cluster_expression(eset, method = "kmeans", k = 3, target = "sample")
print(km_result$clusters)

# DBSCAN clustering
db_result <- cluster_expression(eset, method = "dbscan", eps = 2.0, minPts = 5)
summary(as.factor(db_result$clusters))
}
