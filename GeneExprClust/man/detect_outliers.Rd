% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exploratory.R
\name{detect_outliers}
\alias{detect_outliers}
\title{Detect and optionally remove outlier genes based on variability}
\usage{
detect_outliers(
  eset,
  method = c("mad", "iqr"),
  threshold = 3,
  plot = FALSE,
  remove = TRUE
)
}
\arguments{
\item{eset}{An ExpressionSet object}

\item{method}{Method for variability detection: "mad" (default) or "iqr"}

\item{threshold}{Threshold multiplier (e.g., 3 MADs above median)}

\item{plot}{Logical. If TRUE, shows a distribution plot of variability}

\item{remove}{Logical. If TRUE (default), removes outlier genes from the returned ExpressionSet.}
}
\value{
A list with:
  \item{filtered}{ExpressionSet (original or filtered depending on `remove`)}
  \item{outliers}{Character vector of outlier gene names}
}
\description{
Detect and optionally remove outlier genes based on variability
}
\examples{
data(eset)
# Detect and remove outlier genes using MAD method
eset_filtered <- detect_outliers(eset, plot = TRUE)
}
