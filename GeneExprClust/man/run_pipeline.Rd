% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{run_pipeline}
\alias{run_pipeline}
\title{Complete Gene Expression Analysis Pipeline}
\usage{
run_pipeline(
  data_path = NULL,
  expression_set = NULL,
  data_type = c("csv", "tsv", "eset"),
  pheno_data = NULL,
  output_dir = "pipeline_results",
  normalize = TRUE,
  norm_method = c("log2", "quantile", "zscore", "deseq"),
  batch_correction = NULL,
  filter_low_expr = TRUE,
  filter_threshold = 1,
  filter_min_samples = 3,
  detect_outliers = TRUE,
  outlier_method = c("mad", "iqr"),
  outlier_threshold = 3,
  remove_outliers = TRUE,
  clustering_methods = c("hierarchical", "kmeans"),
  target = c("gene", "sample"),
  k_clusters = 5,
  max_k = 15,
  hclust_method = "ward.D2",
  distance = "euclidean",
  kmeans_nstart = 25,
  dbscan_eps = NULL,
  dbscan_minPts = 5,
  perform_validation = TRUE,
  gap_statistic = TRUE,
  gap_max_k = 10,
  gap_B = 20,
  create_plots = TRUE,
  heatmap_scale = c("row", "none", "column"),
  pca_color_by = NULL,
  save_plots = TRUE,
  perform_enrichment = TRUE,
  gene_sets_source = c("online", "gmt", "example", "custom"),
  gene_sets_collection = "H",
  gene_sets_species = "Homo sapiens",
  gene_sets_file = NULL,
  custom_gene_sets = NULL,
  enrichment_p_threshold = 0.05,
  min_gene_set_size = 5,
  max_gene_set_size = 500,
  verbose = TRUE
)
}
\arguments{
\item{data_path}{Character, path to expression data file or ExpressionSet .RData file}

\item{expression_set}{The ExpressionSet object to be analyzed}

\item{data_type}{Character, type of input data: "csv", "tsv", or "eset"}

\item{pheno_data}{Optional data frame or file path to sample metadata}

\item{output_dir}{Character, directory to save results and plots (default: "pipeline_results")

# Normalization parameters}

\item{normalize}{Logical, whether to perform normalization (default: TRUE)}

\item{norm_method}{Character, normalization method: "log2", "quantile", "zscore", "deseq"}

\item{batch_correction}{Character or vector, batch variable for correction (default: NULL)}

\item{filter_low_expr}{Logical, whether to filter low-expressed genes (default: TRUE)}

\item{filter_threshold}{Numeric, minimum expression threshold (default: 1)}

\item{filter_min_samples}{Integer, minimum samples meeting threshold (default: 3)

# Exploratory analysis parameters}

\item{detect_outliers}{Logical, whether to detect and remove outlier genes (default: TRUE)}

\item{outlier_method}{Character, method for outlier detection: "mad" or "iqr"}

\item{outlier_threshold}{Numeric, threshold multiplier for outlier detection (default: 3)}

\item{remove_outliers}{Logical, whether to remove detected outliers (default: TRUE)

# Clustering parameters}

\item{clustering_methods}{Character vector, methods to use: "hierarchical", "kmeans", "dbscan"}

\item{target}{Character, cluster "gene" (default) or "sample"}

\item{k_clusters}{Integer, number of clusters for hierarchical/kmeans (default: 5)}

\item{max_k}{Integer, maximum k for elbow plot (default: 15)}

\item{hclust_method}{Character, linkage method for hierarchical clustering (default: "ward.D2")}

\item{distance}{Character, distance metric (default: "euclidean")}

\item{kmeans_nstart}{Integer, number of random starts for k-means (default: 25)}

\item{dbscan_eps}{Numeric, DBSCAN epsilon parameter (required if using DBSCAN)}

\item{dbscan_minPts}{Integer, DBSCAN minimum points parameter (default: 5)

# Validation parameters}

\item{perform_validation}{Logical, whether to perform clustering validation (default: TRUE)}

\item{gap_statistic}{Logical, whether to compute gap statistic (default: TRUE)}

\item{gap_max_k}{Integer, maximum k for gap statistic (default: 10)}

\item{gap_B}{Integer, bootstrap samples for gap statistic (default: 20)

# Visualization parameters}

\item{create_plots}{Logical, whether to create visualization plots (default: TRUE)}

\item{heatmap_scale}{Character, scaling for heatmap: "none", "row", "column" (default: "row")}

\item{pca_color_by}{Character, phenotype variable to color PCA by (default: NULL)}

\item{save_plots}{Logical, whether to save plots to files (default: TRUE)

# Enrichment analysis parameters}

\item{perform_enrichment}{Logical, whether to perform pathway enrichment (default: TRUE)}

\item{gene_sets_source}{Character, source for gene sets: "online", "gmt", "example", or "custom"}

\item{gene_sets_collection}{Character, MSigDB collection for online source (default: "H")}

\item{gene_sets_species}{Character, species for online gene sets (default: "Homo sapiens")}

\item{gene_sets_file}{Character, path to GMT file if using "gmt" source}

\item{custom_gene_sets}{List, custom gene sets if using "custom" source}

\item{enrichment_p_threshold}{Numeric, p-value threshold for enrichment (default: 0.05)}

\item{min_gene_set_size}{Integer, minimum gene set size (default: 5)}

\item{max_gene_set_size}{Integer, maximum gene set size (default: 500)

# General parameters}

\item{verbose}{Logical, whether to print progress messages (default: TRUE)}
}
\value{
A list containing all analysis results:
\itemize{
  \item original_data - Original ExpressionSet
  \item processed_data - Processed ExpressionSet after normalization/filtering
  \item outlier_info - Information about detected outliers
  \item clustering_results - Results from all clustering methods
  \item validation_results - Clustering validation metrics
  \item plots - List of generated plots
  \item enrichment_results - Pathway enrichment analysis results
  \item summary - Summary of the analysis
}
}
\description{
This function provides a comprehensive workflow for gene expression analysis,
including data loading, preprocessing, normalization, exploratory analysis,
clustering, visualization, and pathway enrichment analysis.
}
\examples{
data(eset)
results <- run_pipeline(
  expression_set = eset,
  clustering_methods = c("hierarchical", "kmeans"),
  k_clusters = 4
)
}
