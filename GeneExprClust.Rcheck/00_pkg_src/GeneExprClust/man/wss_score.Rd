% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_validation.R
\name{wss_score}
\alias{wss_score}
\title{Compute Within-Cluster Sum of Squares (WSS)}
\usage{
wss_score(data, clusters)
}
\arguments{
\item{data}{Numeric matrix or data frame of expression data (genes x samples)}

\item{clusters}{Named vector of cluster assignments}
}
\value{
Total within-cluster sum of squares (numeric)
}
\description{
Compute Within-Cluster Sum of Squares (WSS)
}
\examples{
# Create sample gene expression data
set.seed(123)
data <- matrix(rnorm(200), nrow = 20, ncol = 10)
rownames(data) <- paste0("Gene", 1:20)
colnames(data) <- paste0("Sample", 1:10)

# Create cluster assignments
clusters <- c(rep(1, 8), rep(2, 7), rep(3, 5))
names(clusters) <- rownames(data)

# Compute WSS
wss_result <- wss_score(data, clusters)
message(paste("Total WSS: ", round(wss_result, 2)))
}
