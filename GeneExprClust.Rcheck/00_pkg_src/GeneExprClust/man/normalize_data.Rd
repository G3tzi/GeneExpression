% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalization.R
\name{normalize_data}
\alias{normalize_data}
\title{Normalize expression data in an ExpressionSet with optional batch correction and filtering}
\usage{
normalize_data(
  eset,
  method = c("log2", "quantile", "zscore", "deseq"),
  batch = NULL,
  filter = FALSE,
  filter_threshold = 10,
  filter_min_samples = 3
)
}
\arguments{
\item{eset}{ExpressionSet object to normalize.}

\item{method}{Normalization method: "log2", "quantile", "zscore", or "deseq".}

\item{batch}{Optional batch factor for ComBat batch correction (default NULL).}

\item{filter}{Logical, whether to filter low-expressed genes (default FALSE).}

\item{filter_threshold}{Numeric, minimum count threshold for filtering (default 10).}

\item{filter_min_samples}{Integer, minimum number of samples meeting threshold (default 3).}
}
\value{
Normalized ExpressionSet.
}
\description{
Supports log2 transformation, quantile normalization, z-score standardization,
DESeq2 median-of-ratios normalization, optional batch correction (ComBat),
and low-expression filtering.
}
\examples{
data(eset)
# Log2 normalization
eset_log2 <- normalize_data(eset, method = "log2")
}
