% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_cluster_profiles}
\alias{plot_cluster_profiles}
\title{Plot average gene expression profiles per cluster}
\usage{
plot_cluster_profiles(eset, clusters, scale = FALSE)
}
\arguments{
\item{eset}{ExpressionSet object}

\item{clusters}{Named vector of cluster assignments for genes}

\item{scale}{Logical, whether to z-score normalize gene expression before averaging (default FALSE)}
}
\value{
ggplot object showing average expression profile per cluster across samples
}
\description{
Plot average gene expression profiles per cluster
}
\examples{
expr_matrix <- matrix(rnorm(1000), nrow = 100, ncol = 10)
colnames(expr_matrix) <- paste0("Sample", 1:10)
rownames(expr_matrix) <- paste0("Gene", 1:100)
eset <- Biobase::ExpressionSet(assayData = expr_matrix)
clusters <- kmeans(expr_matrix, centers = 3)$cluster
plot_cluster_profiles(eset, clusters, scale = TRUE)
}
